---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: forward-auth
  namespace: data
spec:
  forwardAuth:
    address: http://auth-gateway.data.svc.cluster.local:3001/verify
    authResponseHeaders:
      - X-User-Id
      - X-Org-Id
      - X-Permissions
      - X-Email
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: auth-gateway-ingress
  namespace: data
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/api`)
      kind: Rule
      middlewares:
        - name: forward-auth
          namespace: data
      services:
        - name: auth-gateway
          port: 3001
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: task-status-ws-ingress
  namespace: data
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/ws`)
      kind: Rule
      services:
        - name: task-status-service
          port: 3002
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend-ingress
  namespace: data
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: frontend
          port: 3000

