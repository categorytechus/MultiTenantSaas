apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-agent3
  namespace: data
  labels:
    app: worker-agent3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-agent3
  template:
    metadata:
      labels:
        app: worker-agent3
    spec:
      containers:
        - name: worker-agent3
          image: __ECR_AGENT3_IMAGE__
          imagePullPolicy: Always
          env:
            - name: RABBITMQ_URL
              value: "amqp://admin:admin@rabbitmq.data.svc.cluster.local:5672"
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
